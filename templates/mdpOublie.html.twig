{% extends 'base.html.twig' %}

{% block content %}

<div class="pt-2">
    <div class="flex flex-col items-center gap-2 p-2 py-24">
        <div class="flex flex-col gap-3 w-full max-w-md p-8 bg-form bg-opacity-70 rounded-lg text-text">
        {% if emailSend == true %}
            <h2 class="text-3xl font-bold text-title text-center mb-6">Mail envoyer</h2>
            <div>
                <p class="text-sm">
                    Nous vous vous avons envoyer un mail à cette adresse si un compte GureKultura existant correspond.</p>
                    
                <p class="text-sm">
                    Vous trouverez toute les informations pour changer de mot de passe dans le mail envoyer.</p>
            </div>
        {% endif %}
        {% if emailSend == false %}
            <h2 class="text-3xl font-bold  text-center mb-6">Mot de passe oublié ?</h2>
            <form class="space-y-4 gap-2" method="post" action="index.php?controlleur=mdpOublie&methode=lister">
                <div>
                    <input type="email" name="email_to" placeholder="Num. téléphone ou e-mail" 
                        class="w-full p-3 rounded-md bg-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-red-400">
                </div> 
                <div>
                    <p class=" text-sm">
                    Nous vous enverrons un code de vérification à cette adresse ou à ce numéro de téléphone si un compte GureKultura existant correspond.</p>
                </div>
                <input type="submit" Value="Suivant" class="w-full p-3 bg-button text-tbutton font-semibold rounded-md hover:bg-hbutton transition cursor-pointer">
            </form>
            <a href="index.php?controlleur=connexion&methode=lister" class="text-sm  mt-2 block text-center underline">Retour</a>
        {% endif %}
        </div>
    </div>
</div>
{% endblock %}